cmake_minimum_required (VERSION 2.8.11)
project (TestTaskRegula)

#set(${build32} "build\build32")
#set(${build64} "build\build64")

#file(MAKE_DIRECTORY ${build32})
#file(MAKE_DIRECTORY ${build64})

add_subdirectory (src)